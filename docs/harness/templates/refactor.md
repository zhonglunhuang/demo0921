# 重構（general-purpose + worktree；sonnet，跨模組 opus）

```
【目標與動機】
重構 <目標範圍>：<要改善什麼>。
動機：<為什麼值得，如「第 4 處要複製同邏輯，再複製就是第 4 份」>。

【行為保持契約（重構的定義）】
- 對外行為（API 回應、頁面呈現、CLI 輸出）不得改變。
- 動手前先跑 <測試指令> 記錄基線；重構後同組測試必須同樣全綠。
- 測試覆蓋不足：先補「鎖行為」的測試再動；補不了就停下回報，不裸改。

【驗收條件】
1. 基線測試前後皆綠（回報兩次輸出關鍵行）。
2. <重構目標的可驗證形式，如「重複邏輯收斂為一處」>。
3. diff 內不得出現與本重構無關的變更。

【回報格式】
同 implement.md，另附「行為保持證據」：基線 vs 重構後測試輸出對照。
```
